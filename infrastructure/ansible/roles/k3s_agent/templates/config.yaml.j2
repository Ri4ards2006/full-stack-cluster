# Managed by Ansible - K3s Agent Worker Configuration
server: "https://{{ k3s_master_ip }}:6443"
token: "{{ k3s_cluster_token }}"

flannel-iface: {{ tailscale_interface }}
resolv-conf: /etc/rancher/k3s/resolv.conf

